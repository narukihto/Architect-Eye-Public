# Architect-Eye: Public Status Synchronization
# Path: Architect-Eye-Public/.github/workflows/sync_status.yml

name: Sync System Status

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

# إضافة الصلاحيات المطلوبة للـ Push الآلي
permissions:
  contents: write

jobs:
  update-status:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Public Repository
        uses: actions/checkout@v4

      - name: Fetch Health Data
        run: |
          # إضافة معالجة للأخطاء
          status=$(curl -s --max-time 10 https://api.your-sovereign-domain.com/health || echo "Offline")
          echo "$status" > site/status.txt

      - name: Commit and Push Update
        run: |
          git config --global user.name "Architect-Eye-Sync"
          git config --global user.email "sync@architect-eye.com"
          
          if [ -n "$(git status --porcelain)" ]; then
            git add site/status.txt
            git commit -m "chore: System status auto-sync [Auto-Generated]"
            # استخدام الـ token الافتراضي المدمج للتوثيق
            git push origin main
          else
            echo "No status changes detected. Skipping commit."
          fi
